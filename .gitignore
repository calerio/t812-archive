.DS_Store
Thumbs.db
*.tmp
.venv/
__pycache__/

# NEVER commit the Supabase secret/service_role key
.supabase-service-key


# Owner-only MCP config (not needed by contributors)
.mcp.json

# Kept locally but excluded from the public repo + the site (trading cards, bengladino)
excluded/

# Generated print artifacts (run scripts/make_poster.py to regenerate; needs `pip install segno`)
poster.html
poster.pdf
poster-preview.png

# Throwaway local render previews
*-preview.png
